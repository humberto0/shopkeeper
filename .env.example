SERVER_PORT=8080

# Comma-separated list of origins allowed to call the API from a browser.
# "*" allows any origin; restrict this in production.
CORS_ALLOWED_ORIGINS=*

DB_HOST=localhost
DB_PORT=5432
DB_USER=shopkeeper
DB_PASSWORD=changeme
DB_NAME=shopkeeper
DB_SSL_MODE=disable
DB_MAX_CONNECTIONS=10
