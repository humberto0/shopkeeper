pre-commit:
  parallel: true
  commands:
    fmt:
      run: gofmt -l -w {staged_files}
      glob: "*.go"
      stage_fixed: true
    lint:
      run: golangci-lint run

pre-push:
  parallel: true
  commands:
    test:
      run: make test
    govulncheck:
      run: make govulncheck